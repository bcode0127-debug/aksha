# ADR-001: Python for the detection core

Status: Accepted

Latency lives in LLM calls and network hops, not numerics; sklearn/PyTorch hot loops are compiled; ADK is Python-first. C++ only ever for on-board targets, via ONNX export, not hand-writing.
