# ADR-005: Verification is an independent agent

Status: Accepted

Separate call, different model (Gemma), never sees investigator confidence. Disagreement is a first-class outcome (`disputed`, logs, never escalates). Independence is structural, not asserted. Enforced by the `assemble_explainer_input` function node (ADR-013).
