# ADR-003: LLMs never receive raw telemetry arrays

Status: Accepted

Structured summaries only (DetectionResult + summaries). Clarification: the stretch autoencoder consumes raw signals *inside core*; nothing raw ever reaches an LLM, so the stretch does not touch this rule.
